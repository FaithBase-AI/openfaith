{
  "dependencies": {
    "@openfaith/shared": "workspace:*",
    "@rocicorp/zero": "catalog:",
    "drizzle-zero": "0.12.0",
    "effect": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:"
  },
  "license": "MIT",
  "name": "@openfaith/zero",
  "private": true,
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "dev": "zero-cache-dev -p zeroSchema.mts",
    "format": "biome check",
    "generate": "drizzle-zero generate -s drizzle-zero.config.ts",
    "typecheck": "tsgo --noEmit"
  },
  "type": "module",
  "version": "0.1.0"
}
