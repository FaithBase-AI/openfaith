import { atom } from 'jotai'

export const isSmScreenAtom = atom<boolean>(true)
export const isMdScreenAtom = atom<boolean>(true)
export const isLgScreenAtom = atom<boolean>(true)
export const isXlScreenAtom = atom<boolean>(true)
