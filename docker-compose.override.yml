services:
  # OpenFaith Frontend
  openfaith-frontend:
    image: openfaith:latest
    build:
      context: .
      dockerfile: apps/openfaith/Dockerfile
      args:
        VITE_ZERO_SERVER: http://localhost:4848
        VITE_APP_NAME: OpenFaith
        VITE_BASE_URL: http://localhost:3000
        VITE_PROD_ROOT_DOMAIN: localhost
        VITE_PROD_EMAIL_DOMAIN: localhost
        VITE_PLANNING_CENTER_CLIENT_ID: your-pco-client-id-here
    container_name: openfaith-frontend
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      # Database
      - DB_HOST_PRIMARY=postgres
      - DB_NAME=postgres
      - DB_PASSWORD=password
      - DB_PORT=5432
      - DB_USERNAME=user
      # Zero
      - ZERO_UPSTREAM_DB=postgresql://user:password@postgres:5432/postgres
      - ZERO_CVR_DB=postgresql://user:password@postgres:5432/postgres
      - ZERO_CHANGE_DB=postgresql://user:password@postgres:5432/postgres
      - ZERO_REPLICA_FILE=/tmp/zero-replica.db
      - ZERO_AUTH_JWKS_URL=http://openfaith-frontend:3000/api/auth/jwks
      - ZERO_APP_ID=openfaith
      - ZERO_NUM_SYNC_WORKERS=2
      - ZERO_LOG_LEVEL=info
      - ZERO_ADMIN_PASSWORD=admin123
      - ZERO_PUSH_URL=http://openfaith-frontend:3000/api/push
      # Redis
      - REDIS_URL=redis://redis:6379
      # Auth
      - BETTER_AUTH_SECRET=super-secret-key-change-this-in-production
      # Email
      - RESEND_API_KEY=your-resend-api-key-here
      # Planning Center
      - PLANNING_CENTER_SECRET=your-pco-secret-here
      # Frontend specific
      - VITE_ZERO_SERVER=http://localhost:4848
      - VITE_APP_NAME=OpenFaith
      - VITE_BASE_URL=http://localhost:3000
      - VITE_PROD_ROOT_DOMAIN=localhost
      - VITE_PROD_EMAIL_DOMAIN=localhost
      - VITE_PLANNING_CENTER_CLIENT_ID=your-pco-client-id-here
      # Workflow communication
      - WORKERS_HOST=openfaith-workers
      - SHARD_MANAGER_HOST=openfaith-shard-manager
      # Maps
      - VITE_GOOGLE_PLACES_API_KEY=""

    restart: unless-stopped

  # Zero Cache Service
  openfaith-zero:
    image: openfaith-zero:latest
    build:
      context: .
      dockerfile: packages/zero/Dockerfile
    container_name: openfaith-zero
    ports:
      - "4848:4848"
    env_file:
      - .env
    environment:
      # Database
      - DB_HOST_PRIMARY=postgres
      - DB_NAME=postgres
      - DB_PASSWORD=password
      - DB_PORT=5432
      - DB_USERNAME=user
      # Zero
      - ZERO_UPSTREAM_DB=postgresql://user:password@postgres:5432/postgres
      - ZERO_CVR_DB=postgresql://user:password@postgres:5432/postgres
      - ZERO_CHANGE_DB=postgresql://user:password@postgres:5432/postgres
      - ZERO_REPLICA_FILE=/tmp/zero-replica.db
      - ZERO_AUTH_JWKS_URL=http://openfaith-frontend:3000/api/auth/jwks
      - ZERO_APP_ID=openfaith
      - ZERO_NUM_SYNC_WORKERS=2
      - ZERO_LOG_LEVEL=info
      - ZERO_ADMIN_PASSWORD=admin123
      - ZERO_PUSH_URL=http://openfaith-frontend:3000/api/push
      # Redis
      - REDIS_URL=redis://redis:6379
      # Auth
      - BETTER_AUTH_SECRET=super-secret-key-change-this-in-production
      # Email
      - RESEND_API_KEY=your-resend-api-key-here
      # Planning Center
      - PLANNING_CENTER_SECRET=your-pco-secret-here
      # Frontend specific
      - VITE_ZERO_SERVER=http://localhost:4848
      - VITE_APP_NAME=OpenFaith
      - VITE_BASE_URL=http://localhost:3000
      - VITE_PROD_ROOT_DOMAIN=localhost
      - VITE_PROD_EMAIL_DOMAIN=localhost
      - VITE_PLANNING_CENTER_CLIENT_ID=your-pco-client-id-here
      # Workflow communication
      - WORKERS_HOST=openfaith-workers
      - SHARD_MANAGER_HOST=openfaith-shard-manager
    volumes:
      - zero_replica_data:/data
    depends_on:
      - postgres
    restart: unless-stopped

  # Effect Workflow Workers
  openfaith-workers:
    image: openfaith-workers:latest
    build:
      context: .
      dockerfile: backend/workers/Dockerfile
    container_name: openfaith-workers
    ports:
      - "3020:3020"
      - "34431:34431"
    env_file:
      - .env
    environment:
      # Database
      - DB_HOST_PRIMARY=postgres
      - DB_NAME=postgres
      - DB_PASSWORD=password
      - DB_PORT=5432
      - DB_USERNAME=user
      # Zero
      - ZERO_UPSTREAM_DB=postgresql://user:password@postgres:5432/postgres
      - ZERO_CVR_DB=postgresql://user:password@postgres:5432/postgres
      - ZERO_CHANGE_DB=postgresql://user:password@postgres:5432/postgres
      - ZERO_REPLICA_FILE=/tmp/zero-replica.db
      - ZERO_AUTH_JWKS_URL=http://openfaith-frontend:3000/api/auth/jwks
      - ZERO_APP_ID=openfaith
      - ZERO_NUM_SYNC_WORKERS=2
      - ZERO_LOG_LEVEL=info
      - ZERO_ADMIN_PASSWORD=admin123
      - ZERO_PUSH_URL=http://openfaith-frontend:3000/api/push
      # Redis
      - REDIS_URL=redis://redis:6379
      # Auth
      - BETTER_AUTH_SECRET=super-secret-key-change-this-in-production
      # Email
      - RESEND_API_KEY=your-resend-api-key-here
      # Planning Center
      - PLANNING_CENTER_SECRET=your-pco-secret-here
      # Frontend specific
      - VITE_ZERO_SERVER=http://localhost:4848
      - VITE_APP_NAME=OpenFaith
      - VITE_BASE_URL=http://localhost:3000
      - VITE_PROD_ROOT_DOMAIN=localhost
      - VITE_PROD_EMAIL_DOMAIN=localhost
      - VITE_PLANNING_CENTER_CLIENT_ID=your-pco-client-id-here
      # Workflow communication
      - WORKERS_HOST=openfaith-workers
      - SHARD_MANAGER_HOST=openfaith-shard-manager
    depends_on:
      - postgres
      - redis
    restart: unless-stopped

  # Shard Manager
  openfaith-shard-manager:
    image: openfaith-shard-manager:latest
    build:
      context: .
      dockerfile: backend/shard-manager/Dockerfile
    container_name: openfaith-shard-manager
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      # Database
      - DB_HOST_PRIMARY=postgres
      - DB_NAME=postgres
      - DB_PASSWORD=password
      - DB_PORT=5432
      - DB_USERNAME=user
      # Zero
      - ZERO_UPSTREAM_DB=postgresql://user:password@postgres:5432/postgres
      - ZERO_CVR_DB=postgresql://user:password@postgres:5432/postgres
      - ZERO_CHANGE_DB=postgresql://user:password@postgres:5432/postgres
      - ZERO_REPLICA_FILE=/tmp/zero-replica.db
      - ZERO_AUTH_JWKS_URL=http://openfaith-frontend:3000/api/auth/jwks
      - ZERO_APP_ID=openfaith
      - ZERO_NUM_SYNC_WORKERS=2
      - ZERO_LOG_LEVEL=info
      - ZERO_ADMIN_PASSWORD=admin123
      - ZERO_PUSH_URL=http://openfaith-frontend:3000/api/push
      # Redis
      - REDIS_URL=redis://redis:6379
      # Auth
      - BETTER_AUTH_SECRET=super-secret-key-change-this-in-production
      # Email
      - RESEND_API_KEY=your-resend-api-key-here
      # Planning Center
      - PLANNING_CENTER_SECRET=your-pco-secret-here
      # Frontend specific
      - VITE_ZERO_SERVER=http://localhost:4848
      - VITE_APP_NAME=OpenFaith
      - VITE_BASE_URL=http://localhost:3000
      - VITE_PROD_ROOT_DOMAIN=localhost
      - VITE_PROD_EMAIL_DOMAIN=localhost
      - VITE_PLANNING_CENTER_CLIENT_ID=your-pco-client-id-here
      # Workflow communication
      - WORKERS_HOST=openfaith-workers
    depends_on:
      - postgres
    restart: unless-stopped

volumes:
  zero_replica_data:
    driver: local
